{
  "scripts": {
    "hot": "mix watch --hot",
    "dev": "mix",
    "watch": "mix watch",
    "watch-poll": "mix watch -- --watch-options-poll=1000",
    "prod": "mix --production",
    "build:chrome": "zip -r build/chrome-jira-standup-mode.zip . -x './node_modules/*'  -x './.*' -x './build/*'",
    "build:firefox": "zip -r build/firefox-jira-standup-mode.zip . -x './node_modules/*' -x './.*' -x './build/*'"
  },
  "devDependencies": {
    "laravel-mix": "^6.0.49",
    "node-sass": "^7.0.0",
    "resolve-url-loader": "^5.0.0",
    "sass": "^1.55.0",
    "sass-loader": "^12.1.0"
  },
  "prettier": {
    "singleQuote": true,
    "arrowParens": "always"
  }
}
